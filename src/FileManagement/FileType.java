package FileManagement;

/**
 * Classe di tipo enum per elencare i tipi di file per cui è supportata l'esportazione.
 */
public enum FileType {
    
    /**
     * Voce per la selezione dei file di tipo CSV (Comma Separated Value).
     */
    CSV,
    
    /**
     * Voce per la selezione dei file di tipo Testo.
     */
    PlainText,

    /**
     * Voce per la selezione dei file di tipo Seriale.
     */
    Serial;
}
